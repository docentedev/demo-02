{{> Header }}
<div class="container-fluid">
  <div class="container mt-4">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <div class="card-title">Musico {{musico.first_name}} {{musico.last_name}}</div>
          </div>
          <div class="card-body">{{musico.fullName}}</div>
          <div class="card-body">{{musico.id}}</div>
        </div>
      </div>
    </div>
  </div>
</div>
{{> Footer }}
</body>

</html>